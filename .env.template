# Copy this file to .env.local (gitignored) and fill in. Never commit real tokens.
#
# OpenDota needs NO key for the free tier (50k calls/month, 60 req/min); leave blank.
OPENDOTA_API_KEY=
#
# STRATZ GraphQL API requires a free account token (optional cross-check source).
# Get one at https://stratz.com/api
STRATZ_TOKEN=
